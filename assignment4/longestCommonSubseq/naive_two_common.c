#include "max.h"
#include "two_common.h"

int naive_longest_common_two(int array_one[], int len_one, int array_two[],
                             int len_two) {
  for (i = 0; i < len_one; i++) {
    for (j = 0; j < len_two; ++) {
    }
  }
}
